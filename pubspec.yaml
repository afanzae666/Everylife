name: everylife

description: A deep life simulation game built with Flutter and Dart.

publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.13.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.6
  google_fonts: ^8.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
